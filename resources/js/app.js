import './bootstrap';       // if you scaffolded bootstrap.js already
import 'bootstrap';         // this pulls in the JS + Popper

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

